{
  "name": "@orbs-network/tx-simulator",
  "version": "2.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "forge build && cat out/MultiSpy.sol/MultiSpy.json | jq '.deployedBytecode.object' | xargs -I{} sed -i '' 's@const MULTISPY_BYTECODE = .*@const MULTISPY_BYTECODE = \"{}\";@' index.js",
    "test": "node test.js"
  },
  "peerDependencies": {
    "web3": "1.x"
  },
  "prettier": {
    "printWidth": 120
  }
}
